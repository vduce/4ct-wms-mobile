import 'package:flutter/material.dart';

class TenantScopePage extends StatelessWidget {
  const TenantScopePage({required this.child, super.key});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
